# AGENTS.md

## 项目身份

这是夏洛恪英语长期学习系统的唯一活跃项目。当前核心范围是：学校教材进度总控、听力与跟读/口语课程、CloudBase PWA、线上 formal 学情分析、学校练习与考试批改归档、校内错题库和唯一权威学习档案。

背单词、自动出纸面卷、互动课件、ChatGPT 陪练及其他新增功能当前均未立项；未来必须先由家长在 00 窗口讨论并明确批准，才能进入设计或开发。不得把已经归档的旧纸质练习生成工程、互动课件或 ChatGPT 陪练静默恢复为活跃模块。

唯一项目根目录：

`D:\ObsidianVaults\Education\Sherlock\English-Learning`

路径不匹配时禁止写入。

## 每个任务开始前必读

1. `README.md`
2. `docs/00_项目总计划.md`
3. `docs/01_已确认决策.md`
4. `docs/02_PROJECT_STATUS.md`
5. `docs/03_DATA_GOVERNANCE.md`
6. 与任务对应的阶段计划、模块合同和窗口指令

不要依赖聊天记忆替代项目文件。

## 工作范围

允许：

- 听力、跟读和口语课程开发
- 课程 JSON、教材映射、音频生成与校验
- React/Vite PWA、CloudBase 云函数、数据库和云存储
- 科大讯飞 ISE 适配
- 学生端、家长端、成绩与录音管理
- 自动测试、部署、监控和故障处理
- 学校练习、测验和考试照片/扫描件的私密归档与逐题批改
- 校内错题库维护，以及由 00 总控维护的唯一权威学习档案
- 经总控批准后的新模块设计

默认不允许：

- 恢复旧互动课件、自动出纸面卷或 ChatGPT 陪练为活跃工程
- 未立项即开发单词模块或其他新应用
- 删除历史成绩、录音、课程、Git 历史或迁移证据
- 将开发测试写入正式学情
- 在前端暴露家长密码、讯飞密钥、CloudBase 管理密钥或私有结果数据
- 同时维护两套课程源

## 教学规则

- 开学后采用“A 主线 + C 微调”。
- A 主线：只覆盖学校已经教过的教材范围。
- C 微调：约 20% 内容来自 formal 错题、弱词和历史旧坑。
- 每周默认一组：听力一课、跟读一课，教材页段完全一致。
- 2026 秋季开学第一周采用一次性例外：周二至周日每天一组，共 6 组；此例外不自动延续到第二周。
- 每四周可安排一次综合复习，不默认增加新课负担。
- 教材进度不等于掌握结论。

## 数据规则

- 儿童正常入口只允许 `formal`。
- `test` 只能由已认证家长入口产生。
- 完成状态、推荐课和学情分析只认 `formal`。
- 历史无 `data_kind` 记录按 `test` 保守处理。
- formal 与 test 可以共同保存，但必须可查询、可区分、不可串写。
- 录音属于私有学习数据，默认私有存储。
- 学校答卷、扫描件、校内错题库和学习档案只允许存入本地 `private/`，不得进入公开 Git。
- 03 只维护校内错题库；00 是学习档案唯一写入者。单次错误不得自动升级为长期能力结论。

## 工程规则

- 优先沿用现有课程 JSON 与验证规则，迁移时保持兼容。
- 课程内容、音频资产和程序逻辑分离。
- 新功能通过独立模块接入，共享身份、教材、结果模型和家长端。
- 使用成熟库处理 PWA、录音和未来间隔重复等标准能力。
- 修改前检查 Git 状态，保留用户已有改动。
- 重要变更必须运行相关单元、集成和 iPad 验收。
- 本地测试不能替代 iPad Safari 的麦克风、播放和触控验收。

## 部署规则

- CloudBase 与 24 点共用现有免费环境，但使用独立路径、集合、云函数和存储前缀。
- 未经家长确认，不购买套餐、不开启按量付费、不创建第二个付费环境。
- Streamlit 在 CloudBase 开发期仍是 formal 正式入口。
- CloudBase 验收期只能写 test。
- 正式切换时一次性启用 CloudBase formal，同时关闭 Streamlit formal 提交。
- 不允许两个系统同时接收儿童 formal。

## 状态交接

每个阶段完成后必须更新：

- `docs/02_PROJECT_STATUS.md`
- 相关阶段计划的验收状态
- `docs/01_已确认决策.md`，仅在家长作出新决策时更新

部署、迁移或数据操作必须记录提交、版本、URL、验证结果和未完成项。
