# Sherlock English Missions · 角色职责与每周更新流程

> 版本 v1 · 2026-06-11 由听力开发（Claude）起草，家长定稿后对全员生效。
> 本文件是四个角色的共同规约，放在仓库根目录随版本走。任何角色修改本文件须经家长确认。

---

## 一、角色与职责

| 角色 | 载体 | 职责 | 不做 |
|---|---|---|---|
| **家长（summer）** | 人 | 产品负责人 + 唯一信使：所有角色互不直连，信息经家长转发；验收课件；管理密码；iPad 抽查；处理孩子日常成绩回传 | 不写代码 |
| **听力开发（Claude）** | Claude 对话 | 听力教学设计与批改：读成绩→更新学习档案/错题本→按难度规则生成下周 5 份听力课程 JSON + 答案验收文档，写入仓库；维护 `listening/` 引擎与 `tools/make_audio_v2.py`；维护 `listening/CONTRACT.md` | 不碰口语/总首页/app.py；不执行 git |
| **口语开发（Claude 新窗口，2026-06-12 起）** | Claude 对话 | 口语教学设计 + `speaking/` 模块开发（方案见仓库 `02_口语模块方案_v1`）；镜像听力模块的全部模式 | 不改 `listening/` 内部；不执行 git |
| **Codex「英语口语练习」（已退出开发线）** | Codex 对话 | （暂定）仅负责周末 ChatGPT 与孩子口语互动的方案设计 | 不碰仓库任何文件 |
| **发布员（Codex「上传仓库」）** | Codex 对话 | 每周发布：增量生成音频→校验→commit→push→确认 Streamlit 部署→报告 | **不修改任何业务文件**；遇到"需要改文件才能继续"必须停下问家长 |

## 二、文件所有权（谁能改哪里）

| 路径 | Owner | 说明 |
|---|---|---|
| `listening/`、`content/listening/`、`static/audio/listening/`、`tools/make_audio_v2.py` | 听力开发 | 含 CONTRACT.md |
| `speaking/`、`content/speaking/`、`static/audio/speaking/` | 口语开发（Claude） | 镜像 listening 模式；与听力共用 storage/tools |
| 总首页、`app.py`、`storage/`、`tools/` | 听力开发暂管（原架构师职责，统一架构暂缓） | 改 `storage/progress.py` 须保持函数签名（CONTRACT §3） |
| `requirements.txt`、`.gitignore`、`.streamlit/config.toml` | 共有 | 谁改谁在 commit 信息里声明，并经家长转告对方 |
| `01_协作流程与职责.md`（本文件）、`00_部署指南` | 家长定稿 | 修改需家长确认 |

**红线**：a) 跨界改文件必须先经家长转达对方同意；b) 听力模块的儿童端信息隔离（CONTRACT §7）任何整合不得破坏；c) 接口变更必须先改 CONTRACT.md 再改代码。

## 三、每周更新流程（标准节奏，周日发布、周一生效）

```
周一~周五  孩子每天做题 → 复制成绩 → 微信文件传输助手（家长每天只看分数）
周六/周日  ① 家长：把本周 5 段听力成绩粘进 听力部分/成绩回传/W{n}成绩.txt
              → 对【听力开发】说话术 A
           ② 听力开发：批改入档 + 生成下周 5 份听力 JSON（含 open_date 周一~周五）
              + 答案验收文档 → 写入仓库 → 回复话术 B
           ③ 家长：验收听力答案文档 → 对【总架构师】说话术 C
           ④ 总架构师：完成下周口语课件写入仓库 → 回复"口语 W{n+1} 已就绪"
           ⑤ 家长：对【发布员】说话术 D
           ⑥ 发布员：执行发布六步（见第四节）→ 回复发布报告（话术 E 格式）
           ⑦ 家长：iPad 打开网址抽查 1 分钟（能进入、当天课已解锁）→ 本周收工
周一        新一周课程按 open_date 自动逐日解锁，无人值守
```

周中例外（不等周末）：某天听力 <60 分或孩子明显卡住 → 家长当天把该段成绩发【听力开发】，
听力开发可重出**尚未解锁**的课程 JSON 打补丁，之后家长直接走 ⑤⑥（补丁发布）。

## 四、各角色操作规程

### 听力开发（Claude）每周动作
1. 读 `听力部分/成绩回传/W{n}成绩.txt`，逐题批改，更新学习档案与听力错题本（L 编号）
2. 按 02_暑期听力_总体规划 的难度升降规则（每题型独立 L1–L5）定下周配置，给家长一段分析
3. 生成 `content/listening/W{n+1}D01..D05.json`（D05=周测；各带 open_date）+ 家长版答案文档
4. 自检：跑校验器确认 JSON 全部合法
5. 回复家长话术 B，进入等待

### 总架构师（Codex「英语口语练习」）每周动作
1. 收到话术 C 后，开发/更新口语课件写入自己 owner 的目录
2. 若本周有系统级改动（首页/storage 等），在回复中单独声明改了哪些共有文件
3. 完成回复："口语 W{n+1} 已就绪 + 变更摘要"

### 发布员（Codex「上传仓库」）发布七步
1. `cd D:\project_antigravity\education_english\听力部分\sherlock-missions`
2. 运行 `python tools/make_audio_v2.py`（增量生成新课音频；失败则停止并报错给家长）
3. `git status` 自检：确认 `tools/proxy.txt`、`_runtime/`、`secrets.toml` 不在变更里
4. `git add -A && git commit -m "weekly: W{n+1} listening+speaking"`（补丁用 `patch: ...`）
5. `git push origin main`，等待 Streamlit 自动部署（1–3 分钟）
6. **音频完整性抽查**（2026-06-12 新增，#4 教训）：若本次有新增 mp3，在 GitHub 网页
   抽查 ≥2 个新 mp3 的文件大小与本地一致（仓库已加 .gitattributes 防文本化损坏，
   但仍须抽查；不一致 → 重传该文件并复查）
7. 打开应用网址确认能加载（不要实际做题），回复发布报告

## 五、交接话术模板（复制即用）

**话术 A（家长 → 听力开发）**
```
批改 W{n} 听力。成绩在 听力部分/成绩回传/W{n}成绩.txt
```

**话术 B（听力开发 → 家长，交付时）**
```
听力 W{n+1} 已写入仓库：5 份课程 JSON（周一~周五自动解锁）+ 答案验收文档（路径：…）。
本周分析与难度调整：…（哪类题型升/保持/降，为什么）
请你：①验收答案文档 ②转告【英语口语练习】：“听力 W{n+1} 已就绪，请完成口语课件并写入仓库”
③口语就绪后，把话术 D 发给【上传仓库】。
```

**话术 C（家长 → 总架构师）**
```
听力 W{n+1} 已就绪。请完成口语 W{n+1} 课件并写入仓库，不要改动 listening/ 与
content/listening/。完成后回复“口语 W{n+1} 已就绪”，若改了共有文件请单独声明。
```

**话术 D（家长 → 发布员）**
```
执行每周发布 W{n+1}：按仓库根目录 01_协作流程与职责.md 第四节"发布六步"操作，
commit 信息 "weekly: W{n+1} listening+speaking"。只做发布，不修改任何业务文件，
完成后按话术 E 格式报告。
```

**话术 E（发布员 → 家长，报告格式）**
```
发布完成/失败。commit: …；新增/变更文件数: …；音频生成: 新增 x 个/复用 y 个；
确认 proxy.txt 与 _runtime 未入库: 是/否；应用网址可访问: 是/否；异常: …
```

## 六、异常与争议处理

| 情况 | 处理 |
|---|---|
| 发布员报"音频生成失败" | 转给听力开发定位（多为网络/代理或 JSON 文本问题） |
| 发布后应用打不开/报错 | 截图发对应 owner：listening 页面错→听力开发；首页/口语/部署配置错→总架构师；git 层面错→发布员 |
| 两个角色要改同一文件 | 家长裁决，原则：owner 优先，跨界者提需求由 owner 实施 |
| 接口对不上 | 以 `listening/CONTRACT.md` 为准；要改先改文档再改代码 |
| 周日某角色缺位赶不上 | 听力和口语可分开发布（commit 分别为 weekly-listening / weekly-speaking），不互相阻塞 |

## 七、生产环境约束与教训（2026-06-12 首次部署后增补，全员必读）

1. **仓库必须 Public**：音频经 `raw.githubusercontent.com` 直链播放（`listening/audio.py`
   中硬编码仓库地址，换仓库名/分支要同步改它）。衍生铁律：**任何密码、token、
   孩子真实姓名/学校等信息绝不进入该仓库**——密码与 token 只放 Streamlit Secrets。
2. **不要用 `/app/static/` 内部路径**：Community Cloud 不允许浏览器端组件访问该路径
   （本地能跑≠云端能跑），音频统一走 GitHub Raw 直链。
3. **大陆网络对 raw.githubusercontent.com 偶有不稳**：若孩子端报"音频加载失败"，
   备选方案 = 把 audio.py 的 URL 前缀换成 jsDelivr：
   `https://cdn.jsdelivr.net/gh/{用户名}/sherlock-missions@main/`（大陆可达性更好），
   由听力开发改，发布员只发布。
4. **二进制防损**：上传曾出现 mp3 被截损（99K→33K）。已加 `.gitattributes`
   （*.mp3 binary）；发布七步含大小抽查，任何角色不得删除该文件。
5. **占位符规范**：角色间指令中的【】是占位符，填入时去掉括号（曾把【密码】
   连括号写进 Secrets 导致登录失败）。
6. **成绩持久化**：`storage/progress.py` v1.1 起支持 GitHub 私有结果库
   （Secrets：RESULTS_REPO / RESULTS_TOKEN，配置见部署指南第 5 步）。
   该私有库（sherlock-results）任何角色不得公开、迁移或删除；
   架构师做统一持久化改造时需兼容或迁移其中数据。

## 八、本规约的边界

- 教学决策（难度、错题判定、课程内容）：听力归听力开发，口语归总架构师，家长有最终否决权
- 任何角色不得让另一个 AI 角色"代为决策"自己 owner 范围内的事——信息经家长，决策在 owner
- 密码（家长端 PARENT_PASSWORD、GitHub 账号）只在家长手里，不写入任何对话或文件
