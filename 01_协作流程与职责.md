# Sherlock English Missions · 双线程职责与每周更新流程

> 版本 v2 · 2026-06-25 经家长确认改为“唯一 AI + 双工作线程”。
> 唯一 AI 是责任主体；“总控教学”和“在线课件开发”只是上下文分工，不是两个互相独立的角色。

---

## 一、主体与线程职责

| 主体或线程 | 职责 | 边界 |
|---|---|---|
| **家长 summer** | 最终决策、孩子状态确认、课程验收、真机检查、密码与密钥管理 | 不承担 AI 线程间的信息搬运 |
| **唯一 AI 总负责人** | 对教学、内容、开发、验证、发布、数据边界和档案更新承担最终责任 | 不得以“另一个线程没说”为由遗漏同步 |
| **总控教学线程** | 学习档案、教学诊断、暑期主线、纸面材料、ChatGPT 陪练、总工作量、听说结果入档 | 不凭测试数据更新学情 |
| **在线课件开发线程** | 听力、口语、课程 JSON、代码、CONTRACT、音频、私有结果库、发布和线上运维 | 不替代总控教学做学情判定 |

共同规则：

1. 两线程每次开始都读根目录 `01_说明_给AI的操作协议.md` 和 `夏洛恪_英语学习档案.md`。
2. 涉及开发时再读根目录 `03_开发协议_总架构与导航.md`。
3. 数据身份以根目录 `06_测试数据与正式学习记录规则.md` 为准。
4. 跨线程事项由唯一 AI 写入文件或交付说明，不要求家长复制 AI 之间的话术。

## 二、文件主责

| 路径 | 主责线程 | 说明 |
|---|---|---|
| 根目录学习档案、总体规划、纸面材料、ChatGPT 陪练 | 总控教学 | 正式结果才可更新档案 |
| `listening/`、`content/listening/`、`static/audio/listening/` | 在线课件开发 | 修改时同步 `listening/CONTRACT.md` |
| `speaking/`、`content/speaking/`、`static/audio/speaking/` | 在线课件开发 | 修改时同步 `speaking/CONTRACT.md` |
| `app.py`、`storage/`、`tools/`、部署配置 | 在线课件开发 | 改持久化前先保护私有结果库 |
| 本文件、部署指南、README | 在线课件开发 | 流程变化须同步根目录 `03_开发协议_总架构与导航.md` |

**红线**：

- 儿童端信息隔离不得破坏；
- 接口与行为变化必须同步 CONTRACT；
- 测试数据不得写入学习档案；
- 私有结果库清理前必须先出清单并取得家长批准；
- 当前只允许修改或新增文件，不移动、不删除。

## 三、每周更新流程

```
孩子完成正式任务
→ 总控教学线程核对原始证据与数据身份
→ 正式结果入档并形成下周教学要求
→ 在线课件开发线程生成听力/口语课程、答案和音频
→ 自动校验与本地验证
→ 家长验收
→ 在线课件开发线程发布
→ 家长真机抽查
→ 唯一 AI 记录发布结果和遗留项
```

周中若正式成绩明显异常或孩子卡住，总控教学线程先判断是知识问题、题目问题、设备问题还是评分问题，再决定是否修改尚未开放的课程。

## 四、两个线程的操作规程

### 总控教学线程

1. 判断记录是正式、测试、订正、重做还是体验。
2. 只对正式数据进行批改、诊断和错题流转。
3. 更新学习档案并登记更新日志。
4. 形成下周主线、听力、口语和 ChatGPT Mission 的共同教学要求。
5. 控制每日总量；暑期每日 45–60 分钟包含纸面练习。

### 在线课件开发线程

1. 读取总控教学线程留下的正式教学要求。
2. 生成或修改课程 JSON、代码、音频和家长验收材料。
3. 校验 JSON、答案、音频、录次、结果结构和儿童端信息隔离。
4. 修改代码时同步对应 CONTRACT。
5. 家长验收后执行发布七步。

### 发布七步
1. `cd D:\project_antigravity\education_english\听力部分\sherlock-missions`
2. 按变更范围运行听力或口语音频生成工具
3. `git status` 自检：确认 `tools/proxy.txt`、`_runtime/`、`secrets.toml` 不在变更里
4. 复核只包含获批文件，提交信息说明教学周次或补丁目的
5. `git push origin main`，等待 Streamlit 自动部署（1–3 分钟）
6. **音频完整性抽查**（2026-06-12 新增，#4 教训）：若本次有新增 mp3，在 GitHub 网页
   抽查 ≥2 个新 mp3 的文件大小与本地一致（仓库已加 .gitattributes 防文本化损坏，
   但仍须抽查；不一致 → 重传该文件并复查）
7. 打开应用网址确认能加载（不要实际做题），回复发布报告

## 五、双线程交接格式

总控教学线程交给在线课件开发线程：

```
【正式数据范围】
【本周教学结论】
【下周听力要求】
【下周口语要求】
【必须复现的 E/L/口语弱词】
【总工作量限制】
【需要家长确认】
```

在线课件开发线程交回总控教学线程：

```
【修改文件】
【课程与功能变化】
【本地验证】
【线上验证】
【数据影响】
【待家长验收】
【遗留风险】
```

家长只需确认结论、节奏和验收结果，不负责在多个 AI 角色之间复制话术。

## 六、异常与争议处理

| 情况 | 处理 |
|---|---|
| 音频生成失败 | 在线课件开发线程定位网络、代理、JSON 或文本问题 |
| 发布后应用打不开/报错 | 在线课件开发线程按页面、部署、Git 和第三方服务分层定位 |
| 两线程结论冲突 | 以根目录事实源和正式学习证据为准，唯一 AI统一修正文档 |
| 接口对不上 | 以 `listening/CONTRACT.md` 为准；要改先改文档再改代码 |
| 数据身份不明 | 先标“待确认”，不得入档或删除 |

## 七、生产环境约束与教训（2026-06-12 首次部署后增补，全员必读）

1. **仓库必须 Public**：音频经 `raw.githubusercontent.com` 直链播放（`listening/audio.py`
   中硬编码仓库地址，换仓库名/分支要同步改它）。衍生铁律：**任何密码、token、
   孩子真实姓名/学校等信息绝不进入该仓库**——密码与 token 只放 Streamlit Secrets。
2. **不要用 `/app/static/` 内部路径**：Community Cloud 不允许浏览器端组件访问该路径
   （本地能跑≠云端能跑），音频统一走 GitHub Raw 直链。
3. **大陆网络对 raw.githubusercontent.com 偶有不稳**：若孩子端报"音频加载失败"，
   备选方案 = 把 audio.py 的 URL 前缀换成 jsDelivr：
   `https://cdn.jsdelivr.net/gh/{用户名}/sherlock-missions@main/`（大陆可达性更好），
   由在线课件开发线程修改并验证。
4. **二进制防损**：上传曾出现 mp3 被截损（99K→33K）。已加 `.gitattributes`
   （*.mp3 binary）；发布七步含大小抽查，任何角色不得删除该文件。
5. **占位符规范**：角色间指令中的【】是占位符，填入时去掉括号（曾把【密码】
   连括号写进 Secrets 导致登录失败）。
6. **成绩持久化**：`storage/progress.py` v1.1 起支持 GitHub 私有结果库
   （Secrets：RESULTS_REPO / RESULTS_TOKEN，配置见部署指南第 5 步）。
   该私有库（sherlock-results）任何角色不得公开、迁移或删除；
   在线课件开发线程做持久化改造时必须先输出记录清单并保护其中数据。

## 八、当前阶段边界

- 家长拥有最终决策权，唯一 AI 对执行完整性负责。
- 当前孩子尚未正式启动在线训练；已有成绩和录音全部按测试数据处理。
- 私有结果库当前记录清单见根目录 `07_私有结果库记录清单_2026-06-25.md`。
- 2026-06-27：W0 基线卷 + 10 分钟 ChatGPT Mission。
- 2026-06-28：W01D01 听力诊断；状态良好再做 S01D01，否则只体验流程。
- 口语规则：最多 3 次有效评分后允许“先过这题”，保留首读、末读、最高分和每次星级。
- 2B 部署前安全边界：家长真机验收必须从已认证家长 test 入口进入；普通儿童入口固定 formal；
  test 不产生儿童端完成状态。
- W01D01–D05、S01D01–D05 全部保持可见并按编号排列；只标明当前推荐课程，
  2B 不修改课程状态或开放日期。
- 当前只修改或新增文件，不移动、不删除。
- 密码（家长端 PARENT_PASSWORD、GitHub 账号）只在家长手里，不写入任何对话或文件
