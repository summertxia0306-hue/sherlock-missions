# 口语模块方案 v1（待家长确认，未动代码）

> **进度（2026-06-12）**：§6 第 2 步冒烟测试页已开发完毕（`speaking/smoke.py`，
> 入口 `?mode=smoke` + 家长密码），待发布与家长双端实测。操作见
> `03_口语冒烟测试_操作指南.md`。冒烟通过前不开发正式模块。

> 2026-06-12 · 依据家长五项决策制定：①三形式渐进（跟读→问答→看图说话）
> ②AI 评分 = Azure 发音评测 ③录音存私有库 ④每天听力+口语各一课 ⑤口语开发归 Claude。
> 技术底座复用听力模块：同一仓库、同一发布流、同样的课程 JSON / 提交模型 / open_date。

---

## 1. 课程形态（每课 5–8 分钟，约 8 题）

三种题型，比重随周推进（家长决策①）：

| 题型 | 流程 | W1 | W3 | W5+ |
|---|---|---|---|---|
| **repeat 跟读** | 听示范音（Ana，2遍）→ 按住录音 → AI 评分 → 星级反馈，可重录（最多3次取最高） | 6题 | 4题 | 3题 |
| **qa 听问答** | 听问题（不显示文字）→ 录音回答 → AI 判"是否说到目标答案"+发音分 | 2题 | 3题 | 3题 |
| **picture 提示说话** | 屏显提示词/emoji场景 → 自由说1–2句 → AI 转写给家长看+发音分 | 0 | 1题 | 2题 |

- 跟读内容与**当天听力课联动**：当天听力课的核心句+错题句优先编入（听说闭环，家长决策④）
- 示范音频复用听力的音频生产线（edge-tts 片段哈希复用，零新增工具）

## 2. 评分设计（讯飞语音评测 ISE，2026-06-12 改定）

> 变更记录：原选 Azure（B），因家长 Azure 免费账户注册被风控拒绝，改用讯飞。
> 决策依据：讯飞注册无门槛（微信扫码）、评测引擎专为中国学生英语设计（对童声+
> 中式口音适配更好）；唯一短板=美国服务器跨境调国内 API 的延迟稳定性，冒烟测试
> 首项验证，不稳则加自动重试/后台评分补救。

- 服务端（Streamlit 美国服务器）调讯飞 ISE WebSocket API：传录音+目标句 → 返回 准确度/流利度/完整度/逐词得分
- **孩子看到的**：1–3 颗星 + 鼓励语（⭐️再试一次会更棒 / ⭐️⭐️很不错 / ⭐️⭐️⭐️真棒），不显示数字分数
- **家长端与档案看到的**：精确分数、逐词得分（哪个词发音弱）、转写文本
- 星级阈值 W1 从宽（防打击积极性），按他实际分数分布在 W2 校准——童声评分偏差是已知风险，阈值必须实测定
- qa/picture 题：Azure 同时返回转写文本 → 关键词匹配判断"答没答到点"
- 降级路（评分对他不稳时）：转写判词对错 + 家长抽听发音，一键切换不重写课件

## 3. 录音存储（家长决策③）

- 浏览器录音：Streamlit 原生 `st.audio_input`（麦克风组件，iPad Safari 需冒烟实测）
- 录音 wav → GitHub API 写入 **sherlock-results 私有库** `recordings/{course_id}/{日期}_{题号}.wav`
  （复用听力持久化的同一令牌 RESULTS_TOKEN，零新增配置）
- 家长端新增"录音箱"页签：按课列出、在线试听、可标记"已听"
- 隐私：私有库仅家长账号可见；文件名不含真实姓名

## 4. 课件流程（与听力同款交互骨架）

开始页（试音+麦克风权限引导）→ 逐题：听示范/问题 → 录音 → 评分星级 → 交卷成绩单
（总星数+各题星级）→ **📨 提交**（提交才有记录，可重做再提交，与听力一致）→
复制成绩文本（含各题分数，回传给我入档）。
口语没有"订正流"——重录机制（3次取最高）就是它的订正。

## 5. 架构与目录（复用听力骨架）

```text
speaking/
  page.py        课件渲染+录音+星级（镜像 listening/page.py）
  engine.py      流程与计分（纯逻辑）
  models.py      口语课程 JSON 校验器
  ise.py         讯飞语音评测封装（WebSocket，密钥从 Secrets 读）
  recorder.py    st.audio_input 封装 + 私有库上传
content/speaking/W01D01.json …
static/audio/speaking/      示范音频（make_audio_v2 直接支持）
```

- 主界面：app.py 升级为两入口首页「🎧 听力练习｜🗣️ 口语练习」，口语列表同样
  按 open_date 解锁、提交即"已完成"
- Secrets 新增三行：XF_APPID / XF_API_KEY / XF_API_SECRET
- **待家长确认**：口语归 Claude 后，Codex「英语口语练习」对话框还剩什么职责
  （只做星星系统/周报？还是退出）？协作规约与 CONTRACT 等确认后更新

## 6. 实施顺序

1. **家长**：注册 Azure 取密钥（附录A零基础步骤，约15分钟，免费层每月5小时够用）→ 填进 Streamlit Secrets
2. **冒烟测试（先于一切开发）**：我出一个单页测试（录音→Azure评分→私有库上传），
   你在 PC 和 iPad 各测一次——`st.audio_input` 在 iPad Safari 的表现和 Azure 评分
   对童声的靠谱程度，这两个风险必须先验，不过关就改方案
3. 冒烟通过 → 开发 speaking 模块 + W1 口语 5 课（与听力 W1 主题联动）→ 你验收 → 发布
4. 之后并入周流程：每周日听力+口语各 5 课一起出、一起发布

## 7. 风险清单

| 风险 | 等级 | 应对 |
|---|---|---|
| st.audio_input 在 iPad Safari 的兼容性 | 高 | 冒烟测试第一项；不行则自研录音组件（参考限次播放组件的做法） |
| 美国服务器→讯飞国内API 跨境延迟/抖动 | 高 | 冒烟测试第二项；不稳则自动重试1次→仍失败转后台评分（孩子先做下一题，星级稍后出） |
| 讯飞对低龄童声评分偏差 | 中 | 引擎本为中国学生设计，预期优于Azure；星级从宽+W2校准+降级路（仅判词对错） |
| 讯飞免费试用额度具体数量未确认 | 中 | 注册后控制台见真章；不足则购买资源包（几十元量级，先报家长批准） |
| 录音上传慢（wav约300KB/题，私有库API） | 中 | 压缩为16kHz单声道；上传放后台不阻塞下一题 |
| 麦克风权限弹窗吓到孩子 | 低 | 开始页用中文引导"点允许，让小话筒能听到你" |

## 附录A：讯飞开放平台注册步骤（零基础，约10分钟）

1. 打开 https://www.xfyun.cn → 右上角注册/登录（**微信扫码**最快，或手机号）
2. 进入控制台 https://console.xfyun.cn → 首次会引导"创建我的第一个应用"：
   应用名随便填（如 sherlock-english）→ 提交
3. 完成**个人实名认证**（控制台内入口，身份证信息线上提交，一般即时通过）——
   认证后才能领取/申请服务量
4. 在控制台左侧服务列表找到「语音扩展」→「**语音评测（流式版）**」→ 进入服务管理页
   （若有"领取免费试用包"按钮，点领取；记下页面显示的剩余可用量告诉 AI）
5. 同一页面右上方可看到三个鉴权信息：**APPID、APISecret、APIKey**，逐个复制
6. Streamlit 后台 Settings → Secrets 追加三行（值不带任何括号）：
   XF_APPID = "你的APPID"
   XF_API_KEY = "你的APIKey"
   XF_API_SECRET = "你的APISecret"
