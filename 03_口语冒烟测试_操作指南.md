# 口语冒烟测试 · 家长操作指南

> **历史文档：仅供旧 Streamlit 冒烟流程追溯。** 当前 test 验收必须走已认证的 CloudBase 家长入口，具体流程以 `docs/03_DATA_GOVERNANCE.md`、当前阶段计划和 `speaking/CONTRACT.md` 为准。

> 2026-06-12 · 口语开发（Claude）交付。这是 02_口语模块方案 §6 第 2 步的硬闸：
> 三个高风险点（iPad 录音 / 跨境评分延迟 / 私有库上传）**双端实测通过，才开始开发正式口语模块**。

## 一、这次改了什么（发布员与听力开发知悉）

| 文件 | 性质 |
|---|---|
| `speaking/__init__.py` `speaking/ise.py` `speaking/wavtools.py` `speaking/smoke.py` | 新增，口语开发 owner 范围 |
| `app.py` | **+3 行** `?mode=smoke` 路由（家长 2026-06-12 裁决批准，请转告听力开发窗口） |
| `requirements.txt` | 共有文件变更声明：新增 `websocket-client>=1.6`；`streamlit>=1.31` 升至 `>=1.40`（录音组件 st.audio_input 需要） |

无任何 listening/ 与课程数据改动。沙箱已通过：全文件编译 + 转码/鉴权/XML解析单元测试。

## 二、发布（对发布员说）

```
执行补丁发布：按 01_协作流程与职责.md 第四节操作（本次无新增音频，跳过音频生成与抽查），
commit 信息 "patch: speaking smoke test page (+websocket-client, streamlit>=1.40)"。
只做发布，不修改任何业务文件。
```

## 三、测试步骤（PC 一遍，iPad Safari 一遍）

1. 打开 应用网址 + `?mode=smoke`，输家长密码（与家长端同一个）
2. **第0步 配置自检**：5 项应全 ✅（讯飞三项你已填过；缺哪项去 Streamlit Secrets 补）
3. **第1步 录音**：选一句跟读句 → 点话筒录音 → 看是否出现"✅ 录到声音"
   （iPad 首次会弹麦克风权限，点允许）
4. **第2步 评分**：点 🚀 开始评分。**每端至少做 3 次**（看耗时是否稳定），
   其中 1 次**故意乱读**（验证引擎会不会拒识/给低分）
5. **第3步 上传**：点 ☁️ 上传，成功后可去 GitHub sherlock-results 私有库
   `recordings/smoke/` 里点开 wav 确认能播放
6. 页面底部"📨 测试报告"整段复制 → 注明设备 → 微信发回给我

**出错不算失败**——错误信息就是测试结果，照样复制回传，我据此修或换方案。

## 四、判读标准（我拿到报告后按此判）

- 录音：iPad Safari 能录到 ≥0.5 秒、采样率正常 → ① 过
- 评分：3 次全成功且耗时 ≤15 秒、波动可接受 → ② 过；偶发失败 → 加自动重试后再测；
  全挂 → 跨境路线不可行，回到方案重议
- 上传：成功且能播放 → ③ 过
- 三项全过 → 进入正式开发（speaking 模块 + W1 口语 5 课）

## 五、已知边界

- 每次"开始评分"消耗讯飞免费额度 1 次（共 1 万次/90 天，冒烟用量可忽略）
- 星级阈值（75/50）是 W1 从宽的临时值，等孩子真实分数分布后校准
- 冒烟页是家长专用诊断页，正式课件的孩子界面另行开发（孩子不会看到分数）
