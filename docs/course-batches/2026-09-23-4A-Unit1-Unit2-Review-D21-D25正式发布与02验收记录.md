# 4A Unit 1–Unit 2 综合复习 D21–D25 正式发布与 02 验收记录

日期：2026-09-23。唯一儿童 formal 入口继续为 [Streamlit](https://sherlock-missions-pesuyw9p75offrqtdadiag.streamlit.app/)；CloudBase formal 保持关闭。本次未切换入口、未部署 CloudBase formal、未修改家庭 24 点，也未生成 test/formal 学习结果、录音或完成状态。

## 发布范围与版本

- 01 内容提交：`b83a08ddfdf0066d0d419f39c61398c6640d8bb9`，已推送 `main`。包含 L/S4A-T1-W01-D21–D25、5 个 study pack 与 154 项音频。
- 02 工程提交：`5b04f8257080b65df87d2c8e7f22e3519925c05b`，已推送 `main`。新增 D21–D25 独立固定音频路由，扩展生成资产白名单与发布回归，不改业务评分、录音或结果写入代码。
- 音频固定提交：`b83a08ddfdf0066d0d419f39c61398c6640d8bb9`。主地址为 Gcore jsDelivr，回退为 cdn.jsdelivr.net 与 GitHub Raw；D16–D20 及历史课程的固定版本保持不变。
- 正式 URL：`https://sherlock-missions-pesuyw9p75offrqtdadiag.streamlit.app/`。

## 自动验证

- 同步安全：运行 `sync-p3-assets.mjs` 前后，权威 `content/listening`、`content/speaking` 与 `static/audio` 共 2577 个文件逐项 SHA-256 一致，缺失 0、变化 0、新增 0；同步结果为听力 37 门、口语 37 门。
- Python：104/104；Web：110/110；`sherlock-api`：87/87；`score-speaking`：13/13；TypeScript 检查与 Web production build 通过。
- 题量：旧 8 题、新 10 题与新 12 题均通过；D21–D25 为 10/10/12/12/12，前 6 题 repeat、后续 QA；第 9–12 题的评分、proof、录音引用、幂等与门控回归通过。
- 录音组件：正常 rerun 不重新声明 recorder；热部署仅在检测到旧模块时 reload；同一题连续 5 次 rerun 稳定；MediaRecorder 主链路、每次重新取流/停止释放、16 kHz 单声道 PCM WAV、浏览器/Python 双重完整性校验及失败不上传/不评分/不耗录次均通过。
- Streamlit 本地真实渲染：五门听力均渲染试音组件并携带完整固定提交 SHA；五门口语第一题均同时渲染示范音与话筒组件并携带完整固定提交 SHA。测试未点击录音、评分或提交。
- 音频线上：154/154 项从 Gcore 固定提交 URL 返回 200、非空，线上完整 SHA-256 与本地一致；manifest 154/154 收录。
- 推荐逻辑：继续按首个可见且 formal 未完成课程动态计算，没有写死 D21。

## 线上核验与边界

- Streamlit `/healthz` 返回 `{"status":"ok"}`；D21–D25 十个直链均返回 HTTP 200 与 Streamlit shell。
- 浏览器自动化服务在读取动态页面时连续失败，未取得部署后 DOM/iframe 证据。因此当前结论是“代码和发布已完成，线上动态页面验收未完成”，不得写成“工程发布通过”。
- CloudBase 健康接口返回 `formal_enabled=false`、`writes=test-only`、`formal_entry_mode=github-http-only`；本轮没有部署任何 CloudBase 函数或静态 formal 入口。
- 家庭 24 点首页 HTTP 200；本轮只读检查，没有创建或修改家庭 24 点资源。
- 未访问或修改 `private/`、历史成绩、录音、课程 ID、音频路径或 Git 历史；未制造技术验收学习记录。

## 回滚条件

若线上动态页面出现 D21–D25 无法进入、音频不可播放、话筒灰色占位/消失、题量错误或正式结果结构异常，立即停止使用本批课程，并将 Streamlit 部署回退到 `d62b5e3`（D16–D20 已验收状态）。保留内容提交、Git 历史、历史成绩与录音；不得通过删除课程、成绩或录音处理故障。若仅 Gcore 故障，优先使用已配置的 cdn.jsdelivr.net / GitHub Raw 回退，不改课程和正式数据。

## 待验收

- 线上动态 DOM：10 个直链实际显示课程标题与正确题量；五门听力试音 iframe、五门口语示范音与话筒 iframe 实际加载，并确认其参数含固定提交 SHA。
- 真实 iPad：麦克风授权、录音、回听、讯飞评分、题号 9–12 与整课提交。该项未实际执行，明确标记“真机验收待完成”。
