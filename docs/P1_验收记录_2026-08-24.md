# P1 CloudBase 基础架构验收记录

> 验收日期：2026-08-24  
> 环境：`family24-d7gqb6r6m2d722f7a`  
> 边界：本记录只覆盖 P1，不进入 P2；P1 云端只允许 test 写入

## 发布结果

- 云函数：`sherlock-api` 部署成功
- 静态路径：`/sherlock-english/`
- test URL：`https://family24-d7gqb6r6m2d722f7a-1383960965.tcloudbaseapp.com/sherlock-english/`
- 静态文件：12/12 上传成功
- 真实 test 结果 ID：`58d7621e-0fd7-4abb-aaf8-a6ab65e9a5fd`

## 服务端验证

| 验证项 | 结果 |
|---|---|
| health | `ok=true`、`stage=P1`、`formal_enabled=false`、`writes=test-only` |
| 未认证提交 | `UNAUTHORIZED` |
| formal 动作 | `FORMAL_DISABLED` |
| 真实结果类型 | `data_kind=test` |
| 正式完成资格 | `formal_completion_eligible=false` |
| 结果课程 | `P1-SMOKE` |
| 结果模块 | `listening` |

真实结果已从云端 `sherlock_results` 按结果 ID 反查确认；不是仅依据脚本终端输出。

## 静态站与桌面验证

- 首页、`listening/`、`speaking/`、`parent/`、`manifest.webmanifest` 和 `sw.js` 均返回 HTTP 200。
- 桌面浏览器实际加载标题“夏洛恪英语学习”、P1 TEST 提示和听力、口语、家长三个入口。
- 桌面首页浏览器控制台无 error/warn。
- 深链接静态回退文件与首页内容一致。
- 线上 manifest 的 `start_url` 和 `scope` 均为 `/sherlock-english/`，`display=standalone`；首页包含 manifest、iOS standalone 和 viewport 声明。

## 本地质量门

- Web：19/19；statements 90.43%，branches 90%，functions 81.81%，lines 91.66%。
- 云函数：9/9；lines 91.20%，branches 80.82%，functions 85.71%。
- 旧版 Python 回归：40/40。
- TypeScript 类型检查和生产构建通过。
- 项目未配置独立 lint 命令，未把类型检查冒充 lint。

## 套餐与 24 点发布后回归

- 套餐：体验版
- 到期：2027-02-04 23:59:59
- 超额付费：关闭
- 当前周期：2026-08-04 至 2026-09-04
- 用量：0.82 / 3000 点；剩余 2999.18 点
- 24 点云应用：`family24-web-003`，状态 `SUCCESS`
- 24 点公开首页：HTTP 200，标题正常
- 24 点当前静态文件：本地 11/11 与云端大小和 MD5 精确匹配
- 云端非英语文件仍为发布前的 19 个；新增文件全部位于 `sherlock-english/`

## 真实 iPad 验收结论

用户于 2026-08-24 提供真实 iPad 截图并明确确认 P1 验收通过：

- PWA 以无 Safari 地址栏的独立界面正常显示；
- 听力入口可打开，正确显示“课程业务将在 P2 按原合同迁移”；
- 跟读口语入口可打开，正确显示“课程业务将在 P3 按原合同迁移”；
- 两个入口均明确显示 `formal 入口关闭`，符合 P1 数据边界；
- 页面提供“返回本周任务”导航。

P1 的验收目标是基础架构、入口和 test-only 安全链路，不包含正式课件迁移。因此当前没有听力或口语课件是预期状态，不是缺陷。P1 验收通过；本记录不授权进入 P2。

真实 iPad 断网重启未单独留存截图，不把该项表述为已验证；如后续需要，可在 PWA 回归验收中补测。

## 已知风险

- `@cloudbase/node-sdk` 的传递依赖在 `npm audit` 中存在高危公告；自动修复建议会造成 SDK 破坏性降级，本阶段未擅自执行。需要单独评估可升级版本和 CloudBase 兼容性。
- CloudBase 资源点统计可能有延迟，本文件记录的是 2026-08-24 发布后快照。

这个判断最可能错在哪：现有 iPad 截图证明独立界面和两个模块入口正常，但未单独证明断网重启和 Service Worker 更新行为；本次 P1 通过结论不扩展到这些未留证项目。
