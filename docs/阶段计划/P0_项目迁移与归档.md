# P0 项目迁移与归档

> 目标：把新的唯一开发根目录接入现有 Git 历史，完成可审计迁移，不改变线上服务。
>
> 执行状态：**2026-08-24 已完成并通过验证门**。初次 P0 窗口按当时边界没有提交、推送、部署或进入 P1；验收后用户已明确授权把 P0 迁移结果形成一个独立提交并推送 `main`。

## 进入条件

- 已读取根目录 `AGENTS.md`、总计划、已确认决策和项目状态；
- 当前源仓库仍为 `D:\project_antigravity\education_english\听力部分\sherlock-missions`；
- 目标根目录为 `D:\ObsidianVaults\Education\Sherlock\English-Learning`；
- 用户没有发出新的路径或归档范围变更。

## 本阶段范围

- 盘点源仓库、上级教育项目、教材索引、词库、课程、音频、测试、部署文档和运行时数据；
- 查找硬编码绝对路径、Streamlit URL、GitHub Raw/jsDelivr 地址、私有结果库引用和密钥加载方式；
- 把现有 `sherlock-missions` Git 仓库及完整历史迁到新根目录；
- 将本次治理文档纳入同一仓库；
- 运行迁移前后测试、课程和音频清单对账；
- 形成旧项目归档清单，但暂不删除或移动旧目录。

## 不在本阶段

- 不改业务逻辑；
- 不创建 CloudBase 生产资源；
- 不发布新版本；
- 不切换 formal 入口；
- 不删除旧课、旧音频、成绩、录音、私有结果或 Git 历史；
- 不把上级教育项目的无关内容塞入新仓库。

## 安全迁移步骤

1. 记录源仓库 `HEAD`、分支、远端、工作区状态和 Git 对象完整性。
2. 生成源代码、课程 JSON、音频、文档、测试和运行时目录清单；对关键资产生成 SHA-256。
3. 只检查 `.streamlit`、环境变量和 `_runtime` 是否含独有数据，报告键名和数量，不在日志或 Markdown 显示密钥值。
4. 搜索旧绝对路径和外部资源 URL，建立待适配清单。
5. 因目标目录已有本计划文档，不直接向非空目录执行普通 `git clone`。先克隆到同级临时目录，例如 `D:\ObsidianVaults\Education\Sherlock\_staging_sherlock-missions`。
6. 验证临时克隆的 `HEAD`、远端和完整历史与源仓库一致。
7. 将目标目录现有文档做可恢复快照，再以受控方式把临时克隆的 `.git` 和工作树合并到目标目录；发生同名文件时逐个比较，不覆盖较新的治理文档。
8. 在目标目录运行现有测试、JSON 校验和音频存在性检查。
9. 将治理文档加入 Git，形成一次独立迁移提交；未经用户明确要求，本阶段不推送。
10. 更新 `docs/02_PROJECT_STATUS.md`，登记新根 `HEAD`、验证结果和遗留项。

## 归档策略

- P0 结束时旧路径仍保持原位、只读参考；
- CloudBase 正式切换并验收后，再将上级旧项目整体归档到：
  `D:\ObsidianVaults\Education\Sherlock\Archive\education_english_legacy_YYYY-MM-DD`；
- 归档前后记录文件数、总字节数和抽样 SHA-256；
- 原路径最终只保留迁移说明时，必须先取得用户确认；
- 空的旧拟目录 `English-Listen-Speak` 不属于项目事实源，不在 P0 擅自删除。

## 验证门

- 新根目录是有效 Git 仓库；
- `main`、远端和迁移前 `HEAD` 可追溯；
- Git 历史没有重写；
- 原有测试全部通过，或失败项已证明是迁移前既有问题；
- 课程 JSON、音频和关键文档数量对账；
- 新根以外没有被意外修改；
- 旧线上 Streamlit 仍正常。

## 交付物

- `docs/迁移清单_YYYY-MM-DD.md`；
- 硬编码路径适配清单；
- 资产数量与哈希清单；
- 新根 Git 状态与测试报告；
- 更新后的 `docs/02_PROJECT_STATUS.md`。

## 停止条件

发现以下任一情况立即停止写入并报告：源仓库不干净且变更来源无法判断、目标路径与本文件不一致、私有数据仅存在于将被覆盖的位置、Git 历史无法对应、出现需要删除数据才能继续的步骤。

## 执行结果与验收

- 新根已成为有效 Git 仓库，分支为 `main`，HEAD 为 `14d4d659ebc71832cc1a388f79f9bb80c9a974e1`，远端保持原值。
- 源与目标各有 40 个可达提交；可达对象和 56 条不可达对象的清单哈希一致，`git fsck --full --strict` 未发现损坏。
- 原仓库 770 个跟踪文件全部保留；根 README 冲突已逐项处理，旧版另存，没有覆盖新治理入口。
- 课程 JSON 24 份、成品 MP3 312 个、fragments MP3 380 个完成数量与 SHA-256 对账；312/312 成品 MP3 可解码。
- 迁移前后单元测试均为 40/40 通过；目标根 32 个项目 Python 文件编译通过。
- `_runtime`、本机代理配置、缓存和私有结果库数据均明确排除并原地保留，没有显示或复制密钥值。
- 旧线上 Streamlit 成功从休眠页唤醒到任务首页；本轮未进入课程、未提交数据，也未验证录音和评分链路。
- 旧源路径未移动、未删除，迁移后复查工作区仍干净。
- 交付物：`docs/迁移清单_2026-08-24.md`、`docs/硬编码路径适配清单_2026-08-24.md`、`docs/P0_资产SHA256清单_2026-08-24.tsv`、更新后的 `docs/02_PROJECT_STATUS.md`。

验收结论：**P0 验证门达到并获用户通过**。2026-08-24 用户授权独立提交并推送 `main`；同级临时克隆和迁移前快照保留到 P5 正式切换验收完成。推送后确认新根工作区干净，再进入 P1。
