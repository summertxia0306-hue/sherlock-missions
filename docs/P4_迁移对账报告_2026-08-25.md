# P4 历史数据迁移对账报告

> 日期：2026-08-25
> 批次：`p4-20260825-16ca371e`
> 状态：自动迁移与对账通过；2026-08-26 家长端人工验收通过

## 1. 来源与恢复点

- 原私有库：`summertxia0306-hue/sherlock-results`。
- 固定提交：`16ca371ee0efdd2aa3d50e0b7be256aafba1f442`，快照时工作树干净。
- 保留克隆：`D:\ObsidianVaults\Education\Sherlock\English-Learning-P4-legacy-snapshot-20260825`。
- 原始迁移快照：`D:\ObsidianVaults\Education\Sherlock\English-Learning-P4-migration-artifacts-20260825`。
- CloudBase 导入前备份：`D:\ObsidianVaults\Education\Sherlock\English-Learning-P4-cloudbase-preimport-20260825`。
- 三个目录均保留到 P5 正式切换验收完成，不删除、不移动。

原私有库共 425 个文件、35,874,017 字节；规范化清单 SHA-256 为
`8bd4b0c267cc95b0d945591d6dd49de71fe811f532ae5ab36399de0ba0b6abef`。
逐文件清单见 `docs/P4_旧库SHA256清单_2026-08-25.tsv`。

## 2. 结果记录对账

| 项目 | 来源 | CloudBase | 差异 |
|---|---:|---:|---:|
| 全部历史结果 | 143 | 143 | 0 |
| listening | 63 | 63 | 0 |
| speaking | 80 | 80 | 0 |
| formal | 142 | 142 | 0 |
| test | 1 | 1 | 0 |
| 全字段规范化匹配 | 143 | 143 | 0 |

- 142 条来源明确标注 `data_kind=formal`，按原证据保留。
- 1 条缺少 `data_kind`，按治理规则保守归为 test，不计正式完成。
- CloudBase 文档保留结果、错题、订正、分项、星级、弱词、门控、录音引用、原来源记录 SHA-256、来源提交和迁移批次。
- 自动交付时线上结果总数为 148：历史 formal 142，全部 test 6（迁移 test 1 + P1–P3 既有验收 test 5）。2026-08-26 人工验收后总数为 150：formal 仍为 142、test 增至 8；新增 2 条没有进入正式完成或正式汇总。
- 旧 Streamlit 专用展示字段 `result_text` 不写入 CloudBase，以避免命令行和响应体膨胀；它仍原样保留在外部原始快照中。
- 课程映射见 `docs/P4_课程映射_2026-08-25.tsv`；旧课程不会伪装成当前新学期完成状态。

## 3. 录音对账

| 项目 | 来源 | CloudBase | 差异 |
|---|---:|---:|---:|
| 全部 WAV | 422 | 422 | 0 |
| formal | 382 | 382 | 0 |
| test | 40 | 40 | 0 |
| 总字节数 | 35,203,984 | 按逐文件大小核验一致 | 0 |
| 结果引用的唯一录音 | 357 | 357 个引用已改写为私有 file_id | 0 |

CloudBase 私有路径为 `sherlock-english/legacy/{formal|test}/{course_id}/{sha256}.wav`。数据库不保存公开永久 URL；家长端每次播放须经认证云函数换取 600 秒临时地址。

抽取 formal 首/中/末与 test 首/中/末共 6 个文件，从 CloudBase 临时地址重新下载；6/6 的大小与 SHA-256 均和原文件一致。完整样本记录保存在外部迁移目录的 `import-report-dry-run.json`。

## 4. 幂等与异常

- 首次迁移最终核算：导入 143、异常 0；其中中断前已经成功的 2 条由后续执行识别并跳过，其余 141 条补写。
- 再次运行：结果 `would_import=0 / would_skip=143 / anomalies=0`；录音 `would_import=0 / skipped=422`。
- CloudBase 全字段再比对：`matched=143 / anomalies=0`。
- 导入脚本按 `result_id + legacy_source_record_sha256` 判定重复或冲突；冲突会中止，不覆盖既有数据。

## 5. formal/test 与权限验证

- 在线健康检查：`stage=P4`、`formal_enabled=false`、`writes=test-only`。
- 儿童与当前验收写入仍只允许 test；P4 管理迁移仅导入有明确来源证据的历史 formal。
- 家长查询默认 formal；test 必须显式选择，不进入正式完成汇总。
- 未认证 `listParentResults` 返回 `UNAUTHORIZED`。
- 现有家长密码、会话 HMAC 与讯飞密钥在 P4 采用代码更新方式原样保留，未重新生成、未进入仓库或浏览器包。

## 6. 资源与回归

- CloudBase：体验版，到期 `2027-02-04 23:59:59`，超额付费关闭；2026-08-26 验收后核验当前周期使用 24.57/3000 点、剩余 2975.43 点。
- 24 点：`family24-web-003 / SUCCESS`，11/11 文件一致；未改写 24 点资源。
- P4 迁移测试 8/8；`sherlock-api` 33/33；Web 52/52。
- `sherlock-api` 总覆盖率：lines 92.56%、functions 85.25%、branches 75.81%。
- Web 总覆盖率：statements 83.04%、lines 90.01%、functions 80.55%、branches 76.27%。
- TypeScript 检查、PWA 生产构建和 352/352 静态文件上传通过。

## 7. 人工验收结论

自动化不能代替真实家长密码和 iPad 交互。2026-08-26 用户按以下范围完成验收并明确回复“验收通过”：

1. 打开家长端；若看到“新版本已就绪”，先点“立即更新”。
2. 登录后默认显示 formal，确认汇总为 142 条，且页面没有混入 test。
3. 切换 test，确认自动交付时的 6 条仅出现在 test；验收后线上 test 总数增至 8，formal 仍保持 142。
4. 分别按听力/口语、课程和日期筛选，展开至少一条历史明细。
5. 播放至少一条历史口语录音，确认可听；等待约 10 分钟后旧临时地址不可作为永久链接复用。
6. 桌面和 iPad 各完成一次上述查询，不产生新的儿童正式学习记录。

人工验收没有产生新的儿童正式学习结果，也不据此新增学情结论。P4 可形成独立提交并推送；推送后停留在 P4 完成态，未经用户另行授权不进入 P5。
