# P0 硬编码路径与外部依赖适配清单

> 日期：2026-08-24  
> 本文件只记录 P0 发现项，不在本阶段修改业务行为。

## 1. P1/P2 必须适配

| 类别 | 位置 | 当前值/行为 | 后续处理 |
|---|---|---|---|
| 旧本地根路径 | `00_部署指南_零基础版.md:45` | 指向旧 `sherlock-missions` 绝对路径 | P1 建立新运行手册时改为新根；旧指南可标历史 |
| 旧本地根路径 | `01_协作流程与职责.md:77` | `cd D:\project_antigravity\education_english\听力部分\sherlock-missions` | P1 前改为新根或归档旧流程 |
| 公共音频仓库 | `listening/audio.py:18-19` | jsDelivr 与 GitHub Raw 固定到 `summertxia0306-hue/sherlock-missions@main` | P2 决定 CloudBase 音频源后由统一配置替代；切换前保持现状 |
| 公共音频仓库 | `listening/frontend/index.html:51-52` | 前端重复硬编码 Raw/CDN 前缀 | 与 `audio.py` 同步改造，避免两处漂移 |
| 私有结果库 API | `storage/progress.py:175,194` | GitHub Contents API，仓库名来自 `RESULTS_REPO` | P4 数据迁移前保持只读兼容；切换后改为 CloudBase 服务端接口 |
| 私有录音库 API | `speaking/recorder.py:48,80,98` | GitHub Contents API | P3/P4 改为 CloudBase 私有存储和受控临时 URL |
| 冒烟上传 API | `speaking/smoke.py:50` | GitHub Contents API | P3 新冒烟流程只写 CloudBase test，不写 formal |
| 讯飞 ISE | `speaking/ise.py:49` | `wss://` 评测端点由 host/path 组装 | P3 移入云函数服务端；密钥只放环境变量 |

## 2. Streamlit URL 与部署依赖

- 当前生产入口：`https://sherlock-missions-pesuyw9p75offrqtdadiag.streamlit.app`
- 旧上级项目中的记录位置：`03_开发协议_总架构与导航.md:30`、`05_项目接管审计与重构方案.md:167`。
- 新仓库现有 `00_部署指南_零基础版.md` 仍是 Streamlit 历史发布说明。
- 当前 CloudBase 开发期必须继续让 Streamlit 接收唯一 formal；P1/P2/P3 验收只能写 test。
- 正式切换前不得删除、改写或失效化该 URL；P5 才能一次性关闭 Streamlit formal 并转只读。

## 3. GitHub Raw/jsDelivr 路径

当前固定前缀：

- `https://cdn.jsdelivr.net/gh/summertxia0306-hue/sherlock-missions@main/`
- `https://raw.githubusercontent.com/summertxia0306-hue/sherlock-missions/main/`

风险：

- 依赖仓库保持 Public；
- 依赖 `main` 分支即时内容，不是不可变提交 URL；
- CDN 缓存可能造成短时版本差异；
- 新根迁移不改变 GitHub 仓库名，因此 P0 后旧线上仍可工作。

P2 迁移到 CloudBase 静态资源前，必须保持双源播放行为和现有回退顺序，不能在 P0 提前替换。

## 4. 私有配置与密钥加载

只确认键名，不记录值：

- `PARENT_PASSWORD`
- `RESULTS_REPO`
- `RESULTS_TOKEN`
- `XF_APPID`
- `XF_API_KEY`
- `XF_API_SECRET`

加载位置：`listening/page.py`、`storage/progress.py`、`speaking/page.py`、`speaking/smoke.py`、`speaking/recorder.py`。

本地 `tools/proxy.txt` 已被 Git 忽略且未迁移。新根 `.streamlit/config.toml` 只含公开的 server/theme 配置键，没有 Secrets。

## 5. 保留为审计证据、暂不适配

以下绝对路径属于治理或迁移证据，不应机械替换：

- `docs/阶段计划/P0_项目迁移与归档.md` 中的源路径、目标路径、临时克隆示例和未来归档路径；
- `docs/窗口指令/02_系统开发与CloudBase运维窗口.md` 中的本轮源路径与必读路径；
- `docs/P0_资产SHA256清单_2026-08-24.tsv` 中逐文件源路径；
- `docs/迁移清单_2026-08-24.md` 中的执行证据路径。

## 6. 外部仓库依赖

- 公开代码/音频仓库：`summertxia0306-hue/sherlock-missions`。
- 私有成绩/录音仓库：`summertxia0306-hue/sherlock-results`。
- 公开仓库的 Git 历史已迁入新根；私有仓库数据没有复制或修改。
- P4 必须实时导出私有库并按课程 ID、记录数、`data_kind`、时间、录音数量和 SHA-256 对账，不能只依赖 2026-06-25/07-24 的旧清单。

这个判断最可能错在哪：CloudBase 最终的静态资源域名、云函数地址和私有存储访问方式尚未在 P1 实际创建，因此本清单只能指出需要抽象的旧依赖，不能提前给出最终 URL。
