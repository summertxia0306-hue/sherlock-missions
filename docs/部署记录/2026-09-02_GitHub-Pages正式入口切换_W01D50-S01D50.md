# GitHub Pages 正式入口切换部署记录

> 日期：2026-09-02
> 结论：方案 A 已上线，GitHub Pages 已成为唯一 formal 前端入口。

## 上线范围

- 唯一正式入口：`https://summertxia0306-hue.github.io/sherlock-english/`
- 本次新增开放：听力 `W01D50`、口语 `S01D50`
- 历史 `W01D39–49`、`S01D39–49` 保留，不改写既有正式结果。
- 因教材变更撤回的 `L4A-T1-W01-D01–D06`、`S4A-T1-W01-D01–D06` 未进入公开 Pages，也被服务端 formal 校验拒绝。
- CloudBase 后端、数据库、正式结果、私有录音与讯飞评分服务保持原位。
- formal 口语继续使用分块上传；云存储直传仍只允许家长 TEST。

## 发布记录

- 主项目实现：`0251f5c feat: cut over W01D50 formal Pages entry`
- 可回滚切换脚本：`0ad624d ops: add reversible formal entry cutover`
- GitHub Pages 发布仓库：`9b6b16e deploy: make W01D50 formal entry`
- CloudBase `sherlock-api` 已部署新代码并设置 `FORMAL_ENTRY_MODE=github-http-only`。
- CloudBase 旧 `/sherlock-english/` 已替换为迁移页与限定作用域的 Service Worker。

## 自动验收证据

- Web：97/97 测试通过，TypeScript 类型检查与生产构建通过。
- `sherlock-api`：73/73 测试通过。
- `score-speaking`：13/13 测试通过。
- Python：53/53 测试通过；tools：16/16 测试通过。
- 线上听力、口语目录各 12 门，`W01D50.json`、`S01D50.json` 均返回 HTTP 200。
- 撤回课程代表文件在 Pages 返回 HTTP 404；服务端 formal 提交返回 `COURSE_NOT_FORMAL`。
- GitHub HTTP 可创建并继续 formal 会话；旧 CloudBase Event 创建 formal 会话返回 `FORMAL_ENTRY_REQUIRED`。
- 旧网址已显示“正式入口已迁移”；迁移 Service Worker 只清理 `/sherlock-english/` 范围内缓存。
- 家庭 GitHub Pages 根站内容哈希仍为 `37AB7F0BE802D2FB`，未被本次发布改动。
- 自动验收只创建会话及审计事件，没有创建正式课程结果。

## 回滚边界

如需回滚，必须先保留云函数全部现有环境变量，仅将 `FORMAL_ENTRY_MODE` 恢复为 `dual`，再恢复旧 CloudBase 静态前端；不得删除数据库、成绩、录音、源课程或 Git 历史。

## 已知维护风险

两个 CloudBase 云函数沿用既有 `@cloudbase/node-sdk@3.18.3` 依赖链，`npm audit` 仍报告 4 个 high 与 1 个 moderate。自动修复会引入破坏性 SDK 版本变化，因此本次未越权升级；该问题不影响本次入口边界验收，但应另立维护任务处理。
