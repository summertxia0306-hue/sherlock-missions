# P2 音频与 Manifest 对账报告

> 日期：2026-08-24
> 范围：W01D39–W01D50 听力 test 资产
> 线上前缀：`/sherlock-english/audio/listening/`

## 结论

- 课程：12/12。
- 每课成品音频：18 个；总计 216/216 个 MP3。
- 本地总字节：9,443,808；空文件：0。
- 216/216 个 MP3 已由 FFmpeg 完整解码，未发现损坏。
- 构建产物按 Manifest 路径逐项存在。
- 发布后 216/216 个线上文件均为 HTTP 200，且线上字节的完整 SHA-256 与本地发布文件一致。
- 儿童内容副本 12/12 均未包含 `answer`、`transcript`、`tag`、`parent_note`。

## Manifest 口径

源 `static/audio/listening/manifest.json` 的值是音频生成内容指纹，不是 MP3 文件字节哈希。因此：

1. 用 Manifest 对账课程和音频路径集合；
2. 用本地 MP3 实际字节计算 SHA-256；
3. 下载相同线上路径并计算 SHA-256；
4. 216 项逐项比较，结果全部一致。

重复验证命令：

```powershell
node .\tools\verify-p2-online.mjs
```

## 边界

本报告证明资产完整、可下载且字节一致；不代替真实 iPad Safari 的扬声器播放、锁屏返回、Wi-Fi 波动恢复和音频控件布局验收。

这个判断最可能错在哪：CDN 后续若出现区域节点缓存异常，单一网络位置的全量下载仍不能代表所有地区；真实 iPad 验收必须继续执行。
