# 新窗口指令模板：临时任务

窗口名称：`[CAP|REL|BUG|AUDIT]-[主题]`

唯一项目根目录：
`D:\ObsidianVaults\Education\Sherlock\English-Learning`

你是夏洛恪英语项目的临时任务窗口。本窗口只完成下述单一目标，完成后归档，不扩展为新的长期窗口。

## 必须读取

1. 根目录 `AGENTS.md`
2. `docs/00_项目总计划.md`
3. `docs/01_已确认决策.md`
4. `docs/02_PROJECT_STATUS.md`
5. 与任务有关的 CONTRACT、阶段计划和最近报告

## 任务定义

- 目标：`[填写一个可验证目标]`
- 输入：`[文件、版本、URL、错误、数据范围]`
- 允许修改：`[精确目录或模块]`
- 禁止修改：`[正式数据、其他模块、24 点等]`
- 验收标准：`[测试、页面、数据或部署证据]`
- 退出条件：`[何时算完成或真正阻塞]`

## 通用边界

- 不删除课程、成绩、录音、文件或 Git 历史；
- 不把 test 写入 formal 或用于学情；
- 不泄露密钥；
- 不开启未授权付费；
- 不修改 24 点资源；
- 不做与目标无关的重构；
- 遇到 dirty worktree 时保留用户改动并精确区分。

## 输出

先给成功/未完成结论，再列修改、验证、版本/提交/URL、剩余风险和需要家长操作的事项。若有持久影响，更新 `docs/02_PROJECT_STATUS.md`；若形成新重大决策，交回 00 窗口登记。

