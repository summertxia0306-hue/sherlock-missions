# 部署指南（零基础版）

> **历史文档：仅供旧 Streamlit 系统追溯。** 当前唯一正式入口为 CloudBase PWA；部署与运维以 `README.md`、`docs/02_PROJECT_STATUS.md` 和 `docs/窗口指令/02_系统开发与CloudBase运维窗口.md` 为准。不得按本文重新部署 Streamlit formal。

> 目标：把 `sherlock-missions` 文件夹变成一个固定网址的在线应用。
> 全程四步：生成音频 → 上传 GitHub → 部署 Streamlit → 设置密码。约 30–40 分钟，只做一次；
> 之后每加新课只需"生成音频 + 推送更新"两步，5 分钟。

---

## 第 0 步：检查你电脑上有没有 git

按 `Win + R`，输入 `cmd` 回车，在黑窗口输入：

```
git --version
```

- 显示版本号（如 git version 2.x）→ 你有 git，但**仍推荐用下面的 GitHub Desktop 图形界面**，更省心
- 显示"不是内部或外部命令" → 没有，按第 2 步装 GitHub Desktop 即可，不必单独装 git

## 第 1 步：生成音频（必须先做，仓库里要带着音频一起上传）

1. 双击 `tools\生成音频_v2.bat`
2. 首次运行会自动安装依赖（需联网），然后逐条生成 21 个音频（约 2–3 分钟）
3. 看到"全部完成"即成功；音频生成在 `static\audio\listening\W01D01\`
4. 可随便双击一个 mp3 试听验收（应是和配音版课件一样的 Ana/Aria/Guy 声音）

## 第 1.5 步（可选但推荐）：先在本地体验

1. 确认已完成第 1 步（音频已生成）
2. 双击仓库根目录的 **`本地体验.bat`**（首次自动安装 streamlit，需联网）
3. 浏览器自动打开 `http://localhost:8501` → 完整做一遍：试音 → 答题 → 限次 → 交卷 → 复制成绩
4. 家长端：地址栏改成 `http://localhost:8501/?mode=parent`，本地临时密码 `xlk2026`
5. 体验完在黑窗口按 `Ctrl+C` 退出。有要改的地方此时提出，改完再上传

## 第 2 步：上传 GitHub（用 GitHub Desktop，逐步明细）

**2.1 安装并登录（一次性）**
1. 浏览器打开 https://desktop.github.com → 点 **Download for Windows** → 下载完双击安装（全默认，装完自动启动）
2. 首界面点 **Sign in to GitHub.com** → 会跳到浏览器 → 用你的 GitHub 账号登录 → 点绿色 **Authorize desktop** → 浏览器提示"打开 GitHubDesktop"点允许，回到软件
3. Configure Git 界面：姓名/邮箱保持默认 → **Finish**
   - 忘了 GitHub 密码：登录页点 Forgot password 用邮箱重置；连账号都不确定有没有，去 https://github.com/signup 新注册一个也行（5分钟）

**2.2 把文件夹变成仓库（一次性）**
1. 菜单 **File → Add local repository**（或首页 Add → Add existing repository）
2. 点 **Choose...** → 选中 `D:\project_antigravity\education_english\听力部分\sherlock-missions` → 确定
3. 会出现蓝色提示 *"This directory does not appear to be a Git repository. Would you like to **create a repository** here instead?"* → 点 **create a repository** 链接
4. 弹窗里：Name 保持 `sherlock-missions`；Description 留空；**Git ignore 选 None**（仓库里已有现成的 .gitignore，别让它覆盖）；License 选 None → 点 **Create Repository**

**2.3 提交（commit）**
1. 左侧 Changes 列表会列出全部文件（包括音频 mp3，都要勾上，默认全勾）
2. 左下角 Summary 框填 `init`（写什么都行，是给自己看的备注）→ 点蓝色 **Commit to main**

**2.4 发布到 GitHub（push）**
1. 顶部中间出现 **Publish repository** 按钮 → 点它
2. 弹窗确认：Name = sherlock-missions；**不勾 private，保持 Public**
   （⚠️ 2026-06-12 更正：音频走 GitHub Raw 直链，仓库必须公开才能播放。
   因此铁律：任何密码、token、孩子真实信息**绝不**放进这个仓库，只放 Streamlit Secrets）
   → 点 **Publish repository**
3. 进度条走完即成功。验证：浏览器开 https://github.com → 右上角头像 → Your repositories → 能看到 sherlock-missions（带 Private 标签）→ 点进去能看到 app.py 等文件

**2.5 以后每次更新（日常，2分钟）**
1. 本地文件有任何变化（AI 给了新课 JSON、跑了音频生成）后，打开 GitHub Desktop
2. Changes 自动列出变动 → Summary 填一句如 `add W01D02` → **Commit to main**
3. 点顶部 **Push origin** → 完成。线上应用 1–2 分钟自动更新

## 第 3 步：部署到 Streamlit Community Cloud

1. 打开 https://share.streamlit.io 并登录（你已有账号）
2. 点 **New app**（或 Create app）
3. Repository 选 `你的用户名/sherlock-missions`，Branch = `main`，Main file path = `app.py`
4. 点 **Deploy**，等 1–3 分钟构建完成
5. 得到固定网址：`https://xxxx.streamlit.app` ← 这就是发给孩子 iPad 的地址，存入 Safari 书签/主屏幕

## 第 4 步：设置家长密码

1. 应用页面右下角 **Manage app** → 右上角 ⋮ → **Settings** → **Secrets**
2. 填入一行（引号保留，密码自定）：

```
PARENT_PASSWORD = "你的密码"
```

3. Save。家长端入口 = 网址后面加 `?mode=parent`
   （注意：指南/指令里的【】是占位符，填的时候**去掉括号**，引号保留）

## 第 5 步：成绩持久化（一次性约 10 分钟，强烈建议）

不配置的话，成绩和课程状态在服务重启/每次重新部署后清空。配置后永久保存：

1. GitHub 新建一个**私有**仓库 `sherlock-results`（Add a README 勾上，建完不用动）
2. 生成令牌：GitHub 右上角头像 → Settings → Developer settings →
   Personal access tokens → **Fine-grained tokens** → Generate new token：
   - Token name 随意；Expiration 选 1 年（到期要换，先记日历）
   - Repository access → Only select repositories → 选 `sherlock-results`
   - Permissions → Repository permissions → **Contents → Read and write**
   - Generate，复制以 `github_pat_` 开头的令牌（只显示一次）
3. Streamlit 后台 Settings → Secrets，追加两行（原 PARENT_PASSWORD 保留）：

```
RESULTS_REPO = "你的GitHub用户名/sherlock-results"
RESULTS_TOKEN = "github_pat_xxxxxxxx"
```

4. Save 后应用自动重启。验证：家长端"成绩记录"页签顶部出现"☁️ 云端持久化已启用"

## 日常使用（家长 SOP，2026-06-11 定稿）

**每天**：孩子自己完成——打开网址 → 做题 → 复制成绩 → 发微信"文件传输助手"。
你只瞄一眼分数；**<60 分或明显卡住才当天转给 AI**（可中途调课），其余攒到周末。

**每周六/日一次（约 10 分钟，唯一固定动作）**：
1. 把本周 5 段成绩文本粘进 `听力部分/成绩回传/W{n}成绩.txt`，对 AI 说"批改 W{n} 听力"
2. AI 批改入档 → 出下周 5 份 JSON + 答案验收文档（直接写进仓库）
3. 你扫答案文档 → 双击 `tools\生成音频_v2.bat` → GitHub Desktop：Commit + Push
4. 家长端（`网址?mode=parent`）把做完的课设 archived（防重做刷分）

**提交与重做规则（2026-06-12 提交模型）**：孩子交卷后看到成绩单，**点"提交"按钮
家长端才有记录**（不提交=没记录=没做完）；提交后可"再做一次"并再次提交，每次一条记录，
第 2 次起自动标"（第 N 次）"。给 AI 入档只用第 1 次的成绩文本，重做的 AI 只备注不计判定。

## 以后每加新课（第2课、第3课……）的完整循环

每节新课走同一个闭环，五个环节，你动手的只有 ②③④（合计约 5 分钟）：

**① AI 出课（你只需说一句话）**
孩子做完当天课程 → 你把"复制成绩"的文本发给 AI → AI 批改入档、更新错题本 →
AI 按规划（难度升降规则 + 错题复现）直接生成下一课的 `W0xD0x.json`，
写进 `content\listening\`，并附该课的"答案与原文"给你验收。
JSON 加载时自动校验，坏数据会被拒绝，不用担心 AI 写错格式弄崩应用。

**② 生成音频（双击一次）**
双击 `tools\生成音频_v2.bat`。它只生成**新增**的音频：
相同句子（比如复现的错题词）自动复用已有 mp3，不重复生成、不重复花时间。

**③ 本地体验（可选）**
想先过目就双击 `本地体验.bat` 看一眼新课；放心的话可跳过。

**④ 推送上线（GitHub Desktop 三下）**
打开 GitHub Desktop → Changes 已自动列出新 JSON + 新 mp3 →
Summary 填 `add W01D02` → **Commit to main** → **Push origin**。
等 1–2 分钟线上自动更新，**网址永远不变**。

**⑤ 课程上下架（家长端，可选）**
新课默认对孩子可见（open）。如果同时有多节课 open，孩子端会出现选择列表。
建议习惯：孩子做完一课，你在 `网址?mode=parent` → 课程管理 → 把旧课设为
**archived**，保持当天只有 1 节 open——孩子打开网址直接进当天任务，零选择。
（注意：状态存在服务器运行内存，平台重启会全部回到 open；重启后重设一下即可，
这是 v1 已知限制，统一架构做持久化后消失。）

> 一周节奏对照：周一~周四每天一课 + 周五周测课，都是同一个循环。
> AI 侧每周日依据周五周测结果定下周各题型难度（升/保持/降），在 ① 中自动体现。

## 一次性生成一周 5 课（推荐的常规模式）

每天推一课和一周推 5 课，工具完全一样，后者只是把 ①②④ 各做一次：

1. **周日**：你把本周成绩都已回传的前提下，对 AI 说"出下周整周课"→ AI 基于周五周测
   定难度、基于本周错题池排复现，一次生成 5 份 JSON（周一~周四训练 + 周五周测），
   每份都带 `open_date`（如周一课标 2026-06-22）→ **到日子自动对孩子解锁**，
   你不用每天动手，孩子每天打开网址只看到当天该做的课
2. 双击 `tools\生成音频_v2.bat` **一次**——5 课音频一起生成，且跨课相同句子
   （错词复现、常用问句）自动复用，比分 5 天生成更省时间
3. （可选）`本地体验.bat` 抽查
4. GitHub Desktop：Commit → Push，**一次上线一整周**

**教学上的代价要知情**：批量模式下，周二的课在周日就定稿了，没法吸收周一的新错题
（"次日复现"变成"次周复现"）。补偿机制：你每天照常回传成绩，AI 每日登记错题本；
若哪天暴露严重卡点，AI 可以**重出还没解锁的某天课**（改 JSON → 重跑音频 bat →
push，10 分钟），相当于中途打补丁。每周末的难度调节不受影响。

## 常见问题

- **生成音频时报 Cannot connect / Could not contact DNS servers**：电脑连不上微软语音服务器。
  若你开着代理/科学上网工具：在 `tools\` 下新建 `proxy.txt`，写一行代理地址（clash 默认是
  `http://127.0.0.1:7890`，端口看你工具的设置），保存后重跑 bat，开头会显示"代理设置: …"。
  没有代理工具的话：网络设置里把 DNS 改成 `223.5.5.5`（阿里 DNS）后重跑。

- **打开网址显示"Zzzz / app is asleep"**：免费版长时间没人访问会休眠，点 wake up 按钮等 30–60 秒。早上孩子做题前你可以先在手机上打开一次预热
- **音频没声音**：iPad 别开静音键；先点试音按钮（浏览器要求先点击才能出声）
- **服务重启后家长端成绩没了**：没配置第 5 步的持久化才会这样；配置后成绩存私有结果库，重启不丢。复制成绩文本通道始终保留作双保险
- **某个音频播不出来/大小异常**：对照本地与 GitHub 网页上同名 mp3 的文件大小，不一致说明上传时被损坏（已加 .gitattributes 防护），重新上传该文件即可
- **想改家长密码**：Streamlit 后台 Settings → Secrets 改 PARENT_PASSWORD 即可
